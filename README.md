# MobileReality (IoS + Android)

React Native App.

## Setup

`Node` is required to run this add

To setup app you need to run those commands in project's terminal:

```
npm install --global yarn
yarn
```

## Running the project

```
yarn start
```
